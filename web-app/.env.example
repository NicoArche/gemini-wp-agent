# Configuración de Gemini WP-Agent
# Copia este archivo como .env y configura tus valores

# API Key de Google Gemini (Opcional - si no se proporciona, usa el sistema gratuito)
GEMINI_API_KEY=tu_api_key_de_gemini_aqui

# Puerto del servidor (por defecto 3001)
PORT=3001

# Configuración de CORS (URLs permitidas separadas por coma)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,file://

# Configuración de logs (debug, info, warn, error)
LOG_LEVEL=info

# Límites de rate limiting (requests por minuto)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60000

# Configuración de timeout para comandos WP-CLI (en milisegundos)
WP_CLI_TIMEOUT=30000